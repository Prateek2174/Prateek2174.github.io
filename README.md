# Portfolio — Prateek Singh

Personal site. Plain HTML + CSS, single page, no build step.

Deployed via GitHub Pages.

- `index.html` — the whole site
- `styles.css` — site styles (also inlined into `index.html` for self-contained rendering)
- `assets/` — images
